---
category: Layout
---

Tabbed switching between panels. Compose `<Tabs defaultValue="…">` with a `<TabsList>` of `<TabsTrigger value="…">` and a `<TabsContent value="…">` per panel. Control with `value` + `onValueChange`.
