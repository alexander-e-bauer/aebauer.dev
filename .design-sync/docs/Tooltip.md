---
category: Overlays
---

Hover/focus tooltip. Compose `<Tooltip>` with `<TooltipTrigger asChild>` and `<TooltipContent>`. The app (or preview) must be wrapped in `<TooltipProvider>`. Use `delayDuration` to tune the open delay.
