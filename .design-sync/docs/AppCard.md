---
category: Data Display
---

Branded convenience wrapper around `Card` with `title`, `subtitle`, and `icon` slots so you don't hand-assemble the header. Use `dense` for tighter padding and `glass` for a glassmorphism surface. Put body content in `children`.
