---
category: Feedback
---

Inline callout for important messages. Compose `<Alert>` with `<AlertTitle>` and `<AlertDescription>` (an icon before them reads well). `variant="destructive"` tints it with the destructive token for errors.
