---
category: Data Display
---

Surface container for grouped content. Compose from `<CardHeader>` (with `<CardTitle>`, `<CardDescription>`, and an optional `<CardAction>`), `<CardContent>`, and `<CardFooter>`. Renders on the elevated dark card token.
