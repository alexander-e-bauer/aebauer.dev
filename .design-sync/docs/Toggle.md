---
category: Actions
---

Two-state toggle button (pressed / not pressed). Control it with `pressed` + `onPressedChange`, or leave it uncontrolled with `defaultPressed`. `variant="outline"` gives a bordered look; sizes are `sm` / `default` / `lg`. Good for formatting toggles and single on/off options in a toolbar.
