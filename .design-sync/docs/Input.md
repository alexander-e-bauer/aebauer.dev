---
category: Forms
---

Single-line text field styled for the dark surface. Pair with `Label` (via `htmlFor`) for accessible forms. Supports the usual input `type`s (text, email, password, number, search…), controlled `value`/`onChange`, and `disabled`.
