---
category: Actions
---

Primary action control for the Dark Aurora brand. Use `variant` for emphasis — `default` is the aurora-magenta primary, `secondary`/`outline`/`ghost` step down, `destructive` for dangerous actions, `link` for inline text actions. `size="icon"` makes a square icon-only button. Set `asChild` to apply button styling to a custom element such as an anchor.
