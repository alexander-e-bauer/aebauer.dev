---
category: Feedback
---

Toast notification region. Mount one `<Toaster>` near the app root, then trigger toasts imperatively with `toast("…")` from the `sonner` package. Configure `position`, `richColors`, `closeButton`, and default `duration`.
